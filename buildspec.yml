version: 0.2

phases:
  install:
    commands:
      - echo "Install phase - nothing to install"
  build:
    commands:
      - echo "Build phase successful"
      - echo "No deployment performed by CodePipeline"

artifacts:
  files:
    - "**/*"
