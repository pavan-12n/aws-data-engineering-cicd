version: 0.2

phases:
  build:
    commands:
      - echo "=== CURRENT DIRECTORY ==="
      - pwd

      - echo "=== FULL FILE TREE ==="
      - ls -R

      - echo "=== SEARCHING FOR GLUE FILE ==="
      - find . -name "sample_glue_job.py"
